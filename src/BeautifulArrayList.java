/* Conditions for an array 'nums' to be beautiful:
 * 1. nums is a permutation of the integers in the range [1, n]
 * 2. for every 0 <= i < j < n, there's no index k with i < k < j
 *    where 2 * nums.get(k) == nums.get(i) + nums.get(j)
 * 
 * create a beautiful array 'nums' of size n. */

import java.util.*;

public class BeautifulArrayList
{
    public static void main(String[] butterfly)
    {
        
    }
}